
enum States{init,loading,loaded,error}
enum FromScreen{signUp,forgetPassword,cart,products,address,orders,archivedOrders, other,pending}

enum Screen{pending,accepted,active, other}

enum Auth{email, userName, password, confirmPassword, phone}